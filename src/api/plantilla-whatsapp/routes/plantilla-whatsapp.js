"use strict";

/**
 * plantilla-whatsapp router
 */

const { createCoreRouter } = require("@strapi/strapi").factories;

module.exports = createCoreRouter("api::plantilla-whatsapp.plantilla-whatsapp");
