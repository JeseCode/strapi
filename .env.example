# Host
HOST=0.0.0.0
PORT=1337

# Secrets - Copia estos valores desde Railway (Backend Variables)
APP_KEYS=your-app-keys-here
API_TOKEN_SALT=your-api-token-salt-here
ADMIN_JWT_SECRET=your-admin-jwt-secret-here
TRANSFER_TOKEN_SALT=your-transfer-token-salt-here
JWT_SECRET=your-jwt-secret-here

# Database - Copia estos valores desde Railway (DB Variables)
# Opción 1: Usar DATABASE_PUBLIC_URL directamente
DATABASE_PUBLIC_URL=postgresql://PGUSER:PGPASSWORD@PGHOST:PGPORT/PGDATABASE

# Opción 2: O usa las variables individuales (comenta la línea de arriba si usas esto)
# PGDATA=railway
# PGDATABASE=railway
# PGHOST=autorack.proxy.rlwy.net
# PGPASSWORD=tu-password-de-railway
# PGPORT=12345
# PGUSER=postgres

# Strapi
STRAPI_DISABLE_UPDATE_NOTIFICATION=true
STRAPI_TELEMETRY_DISABLED=true

# Browser (si lo necesitas)
BROWSER=chrome

# URL del frontend (si lo necesitas)
URL=http://localhost:3000

# Meta Cloud API - WhatsApp Automation
# Déjalas vacías hasta tener el número aprobado y la app de Meta configurada.
META_WHATSAPP_API_VERSION=v22.0
META_WHATSAPP_PHONE_NUMBER_ID=your-meta-phone-number-id
META_WHATSAPP_TOKEN=your-meta-permanent-access-token
META_WHATSAPP_VERIFY_TOKEN=your-meta-webhook-verify-token
